import { buildPageMetadata } from '@/lib/seo/meta';

export const metadata = buildPageMetadata({
  title: 'Hash Generator',
  description: 'MD5, SHA-1, SHA-256 해시 값을 빠르게 생성하고 복사하세요.',
  path: '/tools/hash',
});
