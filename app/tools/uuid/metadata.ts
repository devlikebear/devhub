import { buildPageMetadata } from '@/lib/seo/meta';

export const metadata = buildPageMetadata({
  title: 'UUID Generator',
  description: 'UUID v4를 대량으로 생성하고 원하는 형식으로 복사하세요.',
  path: '/tools/uuid',
});
