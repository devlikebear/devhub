import { buildPageMetadata } from '@/lib/seo/meta';

export const metadata = buildPageMetadata({
  title: 'Regex Tester',
  description: '정규표현식을 실시간으로 테스트하고 매칭 결과와 캡처 그룹을 확인하세요.',
  path: '/tools/regex',
});
