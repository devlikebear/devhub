import { buildPageMetadata } from '@/lib/seo/meta';

export const metadata = buildPageMetadata({
  title: 'Markdown Preview',
  description: '마크다운 문서를 실시간으로 작성하고 HTML 결과를 확인하세요.',
  path: '/tools/markdown',
});
